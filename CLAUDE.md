# LLM Wiki — スキーマ

このリポジトリは「LLM Wiki」パターン（[llm-wiki.md](llm-wiki.md) 参照）の実装である。
LLM が構築・保守する個人ナレッジベースであり、Obsidian ボールト `メイン/` がその本体。

## 役割分担

- **ユーザー**: ソースの収集（raw/ への投入）、探索の方向づけ、問いを立てる、意味を考える
- **LLM**: wiki の執筆・更新・相互リンク・索引・ログ・整合性維持のすべて

ユーザーは wiki を読む側。`wiki/` 配下・`index.md`・`log.md` は LLM が書き、LLM が保守する。

## 3層構造

| 層 | 場所 | 所有者 | 性質 |
|---|---|---|---|
| Raw sources | `メイン/raw/` | ユーザー | 不変。LLM は読むだけ。**編集・削除は絶対にしない** |
| Wiki | `メイン/wiki/` + `index.md` + `log.md` | LLM | 生成・更新・再構成すべて可 |
| Schema | この `CLAUDE.md` | 共同 | 運用しながらユーザーと合意の上で進化させる |

## ディレクトリ構造

```
メイン/                  ← Obsidian ボールト
  index.md              ← 全ページの索引（内容別カタログ）
  log.md                ← 追記専用の時系列ログ
  QUEUE/                ← 依頼の置き場（非同期インボックス。毎朝の定期タスクが処理）
  raw/                  ← ユーザーが投入する不変ソース
    assets/             ← クリップ記事の画像（Obsidian の添付ファイル保存先）
  wiki/
    sources/            ← ソース要約（raw の1ソースにつき1ページ）
    entities/           ← 固有名詞（人物・組織・製品・場所など）
    concepts/           ← 概念・トピック・テーマ（横断的なまとめ、進化するテーゼ）
    notes/              ← 保存価値のある回答・比較・分析（クエリの成果物）
```

## ドメイン

この wiki は3つのドメインを単一ボールトで扱う。フォルダではなく frontmatter の `domain` で区分する（1ページ複数ドメイン可）。

- `research` — リサーチ・学習
- `business` — 事業・業務知識（やまのすみか関連）
- `personal` — パーソナル（自己記録）

単一ボールトなのはドメイン横断リンク（例: 学習した概念 ↔ 事業判断）が価値の源泉だから。将来、事業 wiki をチーム共有する必要が出たら `domain: business` のページを抽出して分離する。
**注意**: business ドメインで顧客個人情報（氏名・住所・連絡先）は wiki に書かない。必要ならイニシャルや案件名で参照する。

## ページ規約

- 言語は日本語。ソースが英語でも要約・本文は日本語（専門用語は原語併記可）
- ページ名は内容を表す固有名。日本語可
- ページ間の参照は `[[Wikiリンク]]` を使う。リンクは惜しまず張る
- frontmatter:

```yaml
---
type: source | entity | concept | note
domain: [research, business, personal]   # 該当するもののみ、複数可
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: []   # 根拠となる raw ファイル名 or [[sources ページ]]
tags: []
---
```

## ワークフロー

### Ingest（ソース取り込み）

1. ユーザーが `raw/` にソースを置き、処理を指示する
2. ソースを読む（画像入り記事はまず本文、必要に応じて画像を個別に閲覧）
3. 要点をユーザーと簡潔に議論し、強調すべき点の指示を受ける
4. `wiki/sources/` に要約ページを作成
5. 関連する entities / concepts ページを作成・更新する。新情報が既存の記述と矛盾する場合は上書きせず矛盾として明記する
6. `index.md` を更新
7. `log.md` に追記

原則1件ずつ・対話しながら。複数まとめて投入された場合は確認の上バッチ処理も可。

### Query（質問応答）

- `index.md` → 関連 wiki ページ → 必要なら `raw/` まで降りて回答する
- 根拠は `[[リンク]]` で示す
- 保存価値のある回答（比較・分析・発見した繋がり）は `wiki/notes/` への保存を提案する

### QUEUE 処理（非同期インボックス）

`メイン/QUEUE/` はユーザーが依頼を置く場所。毎朝7時のスケジュールタスク（wiki-queue-ingest）が処理する。「QUEUE を処理して」と言われたら即時実行する。

- 記事・PDF・メモ → ingest として処理し、処理後は raw/ へ移動
- 指示が書かれた .md → 指示を実行し、成果物は wiki/notes/ へ
- `_` で始まるファイルは対象外。空なら何もしない（log 追記も不要）
- 処理後は commit + push まで行う

### Lint（健全性チェック）

週1回のスケジュールタスク（wiki-weekly-lint、日曜20時）またはユーザーの指示で以下を点検し報告する:

- ページ間の矛盾
- 新しいソースに追い越された古い記述
- 孤立ページ（被リンクなし）
- 頻出するのに専用ページがない概念
- 張られるべきなのに欠けている相互リンク
- Web 検索で埋められそうなデータギャップ、次に読むべきソースの提案

## index.md / log.md 規約

- **index.md**: カテゴリ別（Sources / Entities / Concepts / Notes）に「リンク + 一行要約」を列挙。ingest のたびに更新
- **log.md**: 追記専用。エントリの形式は `## [YYYY-MM-DD] ingest|query|lint|setup | タイトル`

## スキーマの進化

運用上の改善（フォルダ分割、frontmatter 項目の追加、検索ツールの導入など）は LLM から提案してよい。ユーザーと合意したらこの CLAUDE.md を更新する。
